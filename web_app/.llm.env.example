# LLM 固定配置文件示例
# 实际使用时复制为 web_app/.llm.env 并填写真实密钥
#
# 当前读取优先级（高 -> 低）：
# 1) web_app/.llm.env
# 2) 进程环境变量（export XXX=...）
# 3) web_app/.env.local
# 4) web_app/.env
# 5) 项目根目录 .env
#
# 建议：把稳定配置放到 web_app/.llm.env，避免不同启动方式导致环境变量丢失。

# auto / mify / openai / azure
LLM_PROVIDER=auto

# OpenAI
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Azure OpenAI
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_MODEL=gpt-4

# Mify
MIFY_API_KEY=
MIFY_BASE_URL=https://mify.mioffice.cn/gateway
MIFY_MODEL=mimo-v2-flash
MIFY_PROVIDER_ID=xiaomi
MIFY_TIMEOUT_SECONDS=60
# 可选：透传 Header
MIFY_USER_ID=
MIFY_CONVERSATION_ID=
# 可选值: none / dw / all
MIFY_LOGGING=
# 可选：true / false（映射到 mify_extra.reasoning_content_enabled）
MIFY_REASONING_CONTENT_ENABLED=
